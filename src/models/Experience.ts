export interface Experience {
    id: number;
    company: string;
    description: string[];
    endDate?: string;
    image?: string;
    location: string;
    positionTitle: string;
    startDate: string;
}